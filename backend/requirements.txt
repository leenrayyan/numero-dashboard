# Production-safe deps used by the deployed Render service.
# Smart Query (vanna + chromadb + gemini) is excluded — it needs a persistent
# vector store that Render's free tier doesn't provide.
# For local development with Smart Query, also install requirements-dev.txt.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
redis>=5.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
